#include <stdio.h>
#include <conio.h>

int main()
{
    char ch;
    char str[100];
    char buffer[100];

    // // getchar() - Read a single character
    // printf("Enter a character: ");
    // ch = getchar();
    // printf("You entered: ");
    // putchar(ch);
    // printf("\n");

    // // gets() - Read a string (deprecated, unsafe)
    // printf("Enter a string (without spaces): ");
    // gets(str); // Unsafe, should use fgets() instead
    // printf("You entered: ");
    // puts(str);

    // // putchar() - Print a single character
    // printf("Printing a character using putchar(): ");
    // putchar(ch);
    // printf("\n");

    // // puts() - Print a string
    // printf("Printing a string using puts():\n");
    // puts(str);

    // // getch() - Read a character without echo (requires conio.h)
    // printf("Press a key: ");
    // ch = getch();
    // printf("\nYou pressed: ");
    // putchar(ch);
    // printf("\n");

    // // getche() - Read a character with echo (requires conio.h)
    // printf("Press a key (with echo): ");
    // ch = getche();
    // printf("\nYou pressed: ");
    // putch(ch);
    // printf("\n");

    return 0;
}
